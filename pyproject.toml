[project]
name = "on-prem-deploy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ansible~=10.7.0",
    # work around https://github.com/ansible/ansible-lint/issues/4533
    "ansible-compat==24.10.0",
    "ansible-lint~=25.1.0",
    "yamllint~=1.37.1",
]
